{
  "name": "dockernode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"docker build -t dockernode .",
    "delete":"docker rm backend --force",
    "container": "docker container run -d --name backend  -p 5000:5000 dockernode"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mysql2": "^2.3.3-rc.0"
  }
}
